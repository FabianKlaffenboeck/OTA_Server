package at.fklab.ota_server.development

import at.fklab.ota_server.models.User

val sampleUsers = listOf<User>(
//    User(id = 1, accessToken = sampleAccessTokens[0], info = ""),
//    User(id = 2, accessToken = sampleAccessTokens[1], info = ""),
)