package at.fklab.ota_server.routes

import io.ktor.server.routing.*


fun Route.login() {
//    route("/login") {
//        findUserByCredentials(call.receive<UserPasswordCredential>())?.let { u ->
//            call.respond(Token(JwtConfig.makeToken(u)))
//        } ?: call.respond(HttpStatusCode.Unauthorized)
//    }
}